# ShowsHub

Aplicatie Avalonia - demo Netflix + IMDb.

Run:

```powershell
dotnet restore
dotnet run --project ShowsHub
```

Funcționalități: MVVM, SQLite (AppData), schimbare temă, login/register UI, favorites/watchlist (schelet), căutare, detalii.
